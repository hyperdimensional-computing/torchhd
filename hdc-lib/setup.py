from setuptools import setup

setup(name="hdc-lib",
      version='0.1', 
      description='A python library for hyperdimensional computing', 
      url='', 
      author='', 
      author_email='', 
      license='MIT', 
      packages=['hdc-lib'], 
      zip_safe=False)